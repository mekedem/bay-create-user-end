import React from "react";
// import { getServiceRequest } from "../../../actions/serviceReqAction";

const ServiceRequest = () => {
    return (
        <div>
            centered at service management centered at service management centered at service management centered at service management
        </div>
    );
};

export default ServiceRequest